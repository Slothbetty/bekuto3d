{
  "name": "svg-to-stl-api",
  "version": "1.0.0",
  "type": "module",
  "description": "API server for converting SVG files to STL using bekuto3d Vue components",
  "main": "svg-to-stl-server.js",
  "scripts": {
    "start": "node --max-old-space-size=4096 --expose-gc svg-to-stl-server.js",
    "dev": "node --watch svg-to-stl-server.js",
    "build": "echo 'No build step required'",
    "test": "node test-client.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "jsdom": "^26.0.0",
    "three": "^0.174.0",
    "form-data": "^4.0.0",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0 <24.0.0"
  }
}
